# 採購與庫存水位儀表板 - 開發規範

## 1. 系統架構與技術棧 (Stack)

- **前端環境**：使用 React 搭配 Tailwind CSS 進行 UI 排版。
- **後端環境**：使用 Node.js (Express) 作為 API 伺服器。
- **資料庫**：連接實體 MS SQL Server。
- **圖表套件**：統一使用 `Recharts` 套件來實作動態圖表。

## 2. 資料庫與安全規範 (Database & Security)

- **資料庫寫入禁令（唯讀架構）**：
  - 本系統為純讀取（Read-Only）儀表板，嚴禁生成任何包含 `INSERT`、`UPDATE`、`DELETE` 或 `DROP` 的 SQL 陳述式或 ORM 寫入邏輯。
  - 所有數據撈取必須透過唯讀的 SQL 查詢、資料庫視圖 (Views) 或特定的預存程序 (Stored Procedures)。
- **敏感資訊與環境變數規範 (Environment Variables)**：
  - 嚴禁在任何程式碼中寫死（Hardcode）MS SQL 連線帳密、API Key、Token 或任何私密金鑰。
  - 所有敏感資訊必須統一存放於根目錄的 `.env` 檔案中。
  - 程式碼中一律必須透過環境變數（如後端的 `process.env.DB_PASS`）來讀取這些值。
  - 專案中必須建立並維護一個 `.env.example` 檔案（不含真實帳密），明確列出系統所需的所有環境變數名稱，以供團隊參照。
- **SQL 查詢安全性**：
  - 執行 SQL 查詢時，若涉及任何前端傳入的參數（如日期篩選、品號、採購員篩選），必須使用「參數化查詢（Parameterized Queries）」，絕對不允許直接使用字串拼接，以防止 SQL 注入（SQL Injection）。
  - 對於複雜的庫存計算，優先使用資料庫的預存程序（Stored Procedure）或視圖（View），並在註解中說明資料表結構。

## 3. 後端 API 與數據處理 (Backend API)

- **API 路由命名**：遵循 RESTful 風格，例如 `/api/inventory/water-level`。
- **數據聚合與運算**：
  - 「低於水位佔比 %」與「水位趨勢線數據」等複雜的聚合與百分比計算，應在後端（或 SQL 端）計算完成後，再將乾淨的 JSON 數據回傳給前端，避免讓前端瀏覽器進行大數據量的計算。
- **錯誤處理 (Error Handling)**：
  - 所有資料庫操作必須包裹在 `try-catch` 區塊中。
  - 發生連線或查詢失敗時，後端必須使用 `console.error` 記錄詳細錯誤，但回傳給前端的 API 回應只能顯示安全的錯誤提示（例如：`{ success: false, message: "無法取得庫存數據" }`）。

## 4. 前端 UI 與動態圖表規範 (Frontend & Charts)

- **圖表呈現**：
  - 「低於水位佔比 %」：使用 Recharts 的 **PieChart（圓餅圖）** 或 **RadialBarChart（環形圖）** 呈現。
  - 「水位趨勢線」：使用 Recharts 的 **LineChart（折線圖）** 或 **AreaChart（面積圖）** 呈現，且必須具備動態漸層或動態載入動畫。
- **資料驅動與重新整理**：
  - 圖表元件必須支援動態重新整理（例如：提供一個「重新整理」按鈕，或使用 React 的 `useEffect` 在元件載入時自動打 API 抓取最新數據）。
  - 當 API 還在載入數據時，UI 必須顯示 Loading 狀態（如 Skeleton 骨架屏或轉圈動畫），不可讓畫面留白。

## 5. 程式碼風格與註解 (Code Style)

- **語言偏好**：使用 JavaScript (ES6+)。
- **註解規範**：所有關鍵的業務邏輯、SQL 查詢邏輯、圖表數據轉換格式，都必須附上簡短的**繁體中文註解**。

## 6. 開發協同與 AI 模型切換規範 (AI Models)

- **AI 模型切換提醒**：
  - **前端 UI 與樣式調整**：使用 **Gemini 3.5 Flash**。其高速反應能提供最即時的排版與 UI 刻劃體驗。
  - **複雜 SQL、API 架構與安全防禦**：手動切換至 **Claude Sonnet** 或 **Gemini 3.1 Pro**。其深度思考（Thinking）能力可確保防範 SQL 注入，並寫出高效能的參數化查詢。
  - **切換時機**：當開發任務從「前端介面調整」轉向「核心邏輯與資料庫安全性」時，AI 助理應主動提醒使用者切換模型，且模型切換後應無縫承接上下文。

## 7. 專案開發日誌規範 (Development Logs - 論文研究版)

- **自動記錄機制 (強制嚴格執行)**：
  - 每當我們完成「一次對話回合 (Turn)」中的功能調整、UX 優化或除錯後，在準備回覆使用者之前，AI 助理必須**強制、主動且立即**追加記錄至專案的 `d:\agy\SMARTPO\docs\dev_log.md` 檔案中。
  - **嚴禁**等待整個階段 (Phase) 開發完畢才整併記錄，以避免遺失中途的開發除錯細節與時間斷層。
- **日誌格式規格 (論文實證數據)**：
  - 日誌必須採用 Markdown 格式往下追加 (Append)，內容嚴格包含以下項目：
    1. **基本資訊**：日期（YYYY-MM-DD）/ 時間（HH:MM）。
    2. **功能項目**：開發功能名稱。
    3. **功能概述**：此功能完成的具體內容。
    4. **AI 協同角色**：【需求梳理 / 代碼生成 / 架構設計 / 除錯】。
    5. **任務複雜度與實際耗時**：
       - 任務複雜度：【低 / 中 / 高】（評估標準：低＝單一組件調整，中＝包含圖表渲染與 API 串接，高＝跨模組架構設計）。
       - AI 實際耗時：實際進行 Prompt 溝通、程式碼生成與部署的總時間（例如：20 分鐘）。
       - 傳統工時估算（由開發者校實）：由開發者（您）依據自身開發經驗填寫，作為論文對比基準（AI 僅提供參考，以確保數據的學術客觀性，避免口委質疑）。
    6. **關鍵提問與 AI 核心回覆紀錄 (Prompt & Response Logs)**：真實記錄開發者於該階段提出的核心疑問/技術諮詢（如：編碼格式疑慮、開發模式選取），以及 AI 給出的核心邏輯、概念澄清或技術架構解析（作為您論文中「人機交互心智模型」與「知識共創」機制的直接文本佐證）。
    7. **技術賦能說明**：記錄 AI 協助跨越的技術盲區或學習點（例如：Recharts 屬性應用、Express 中介軟體設定）。
    8. **遇到之問題、幻覺或誤解**：真實記錄 Bug、環境問題，或 AI 理解錯誤、產生幻覺的具體情況（若無則寫「無」）。
    9. **解決方式與花費時間**：詳細記錄解決方案、**解決該問題實際花費的時間（分鐘/小時）**，以及 AI 是否有進行主動安全防護干預（如自動重構為參數化查詢以防範 SQL 注入）。

## 8. SDD 規格書與原型驅動規範 (System Design Document & Specs Alignment)

- **SDD 唯一真理來源 (Single Source of Truth)**：
  - 本專案所有開發均嚴格遵循「規格先行」原則。在開始編寫實體程式碼前，必須在 `.antigravity/Doc/` 或 `docs/` 下存在對應的 SDD（系統設計規格書）。
  - 開發過程中若因實務限制修改了欄位、邏輯或業務規則，必須**優先**同步修改對應的 SDD 文件，絕不允許程式碼與規格書脫鉤。
- **資料庫與視圖規格對齊 (DB Views Specs Alignment) ⚠️ 主動防護**：
  - 每當涉及實體資料庫連線、新增 SQL View 或 Stored Procedure 時，AI 助理**必須主動提醒使用者**並建立/更新專屬的資料庫規格書（如 `docs/db_views_spec.md`），記錄 View 的功能、實體 SQL 語句、對應的 SAP B1 底層 Table 欄位關係與業務公式，確保「資料庫結構代碼化與文件化」。
- **AI 開發過程主動提示機制 (Proactive Spec Alignment Prompts)**：
  - 在進入每個階段開發、規劃新功能或對接真實 API 時，AI 助理**必須主動提醒使用者**針對 SDD 開發方式需要進行的規格準備。提示情境範例：
    * *「在我們開始寫這個 API 之前，我們是否需要先在 `db_views_spec.md` 中為這個查詢定義好 View 的 Schema 與過濾邏輯？」*
    * *「這項 UI 的新增欄位，是否已在 SDD 規格書中補上？需要同步去更新後端 API 的資料交換 Schema 嗎？」*
  - 透過主動干預與提示，確保所有開發動作均伴隨對應的「規格代碼化 (Docs as Code)」實證文件，以落實論文研究所需的嚴謹軟體工程與人機協同流程。

## 9. Git 版控與更新紀錄規範 (Git Version Control & History)

- **版控歷史同步記錄 (強制嚴格執行)**：
  - 為了保存論文研究的完整開發與版本演進歷程，每次專案進行 `git push` 或產生新的 `git commit` 後，AI 助理必須**主動且強制**將最新的 Git 提交歷史同步更新至 `Docs/git_commit_history.md` 檔案中。
  - **更新格式要求**：需維持 Markdown 表格格式（包含日期、提交哈希值、作者與提交訊息），確保論文寫作與研究追蹤時具備高可讀性與一致性。
  - **人機協同防漏**：若使用者要求進行 `git commit / push`，AI 助理在執行該動作的同一對話回合內，必須自動接續完成 `git_commit_history.md` 的更新，落實自動化文檔紀錄。
